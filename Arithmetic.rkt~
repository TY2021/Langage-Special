(define succ (lamda (x) (+ x 1)))
(define plus (lamda (y) (lamda (x) (+ x y))))
(define id (lamda (x) x))
(define foo (plus 1))
(define bar (lamda (f) (lamda (x) (f (f x)))))
(define baz (lamda (f) (lamda (x y) (f x y))))
(define zee (lamda (f) (lamda (x) (f x y))))
(define boo (baz +))
(define bee (baz *))
